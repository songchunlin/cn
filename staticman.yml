comments:
  allowedFields: ["name", "email", "url", "message"]
  branch: "gh-pages"
  commitMessage: "Add Staticman data"
  filename: "entry{@timestamp}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
  moderation: true
  name: "北河以北"
  path: "https://api.staticman.net/v2/entry/songchunlin/cn/blob/gh-pages/_includes"
  requiredFields: ["name", "email", "message"]
  transforms:
